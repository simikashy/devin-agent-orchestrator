services:
  orchestrator:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEVIN_API_KEY=${DEVIN_API_KEY}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    volumes:
      - ./asoc.db:/app/asoc.db
    restart: unless-stopped
